const dater = (data) => {
    const insert = new Date(data)
    const y = insert.getFullYear();
    const m = insert.getMonth();
    const d = insert.getDay();
    const hr = insert.getHours();
    const min = insert.getMinutes();
};